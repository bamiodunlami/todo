<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous" />

    <!-- google font -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Mulish:wght@400;600;800;600&display=swap" rel="stylesheet" />

    <!-- google icon -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" />

    <link rel="stylesheet" href="/style/index.css" />
  </head>
  <body>
    <div class="container">
      <div class="row justify-content-center todo-container">
        <div class="col-md-6">
          <h3 class="text-center alert alert-primary"><%=user.fname%>'s Todo List</h3>

          <!-- Todo -->
          <div class="todo-form">
            <h4 class="text-center">Create a todo</h4>
            <%if(messages.error){%>
            <div class="alert alert-danger"><%=messages.error%></div>
            <%}%>
            <form action="/todo/add" method="POST" class="row">
              <div class="mb-2">
                <input type="text" class="form-control" id="title" name="title" placeholder="Enter Todo Title" required />
              </div>
              <div class="mb-2">
                <textarea name="description" name="description" id="description" class="form-control" required placeholder="Enter todo description"></textarea>
              </div>
              <button type="submit" class="btn btn-primary w-100">Add Todo</button>
            </form>
          </div>

          <!-- todolist -->
          <div class="todo-list">
            <h4 class="text-center">Your todos</h4>
            <div class="text-center sort-tab">
              <a class="btn btn-outline-secondary" href="/todo/sort/pending">Pending</a>
              <a class="btn btn-outline-secondary" href="/todo/sort/completed">Completed</a>
            </div>
            <div class="row todo-items">
              <%if(todos.length === 0){%>
              <p class="text-center">No todos yet. Create one!</p>
              <%} else { %> <% for(const todo of todos) {%>
              <div class="col-6 mb-2">
                <div class="card">
                  <div class="card-body">
                    <h5 class="card-title"><%= todo.title %></h5>
                    <p class="card-text"><%= todo.description %></p>
                    <button class="btn btn-light">Status: <%=todo.status%></button>
                    <%if(todo.status === "completed"){%>
                    <a class="btn btn-danger" href="/todo/delete/<%= todo.todo_id %>"> Delete</a>
                    <%}else{%>
                    <a class="btn btn-primary" href="/todo/complete/<%= todo.todo_id %>"> Complete</a>
                    <%}%>
                  </div>
                </div>
              </div>
              <%}%> <%}%>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.min.js" integrity="sha384-VQqxDN0EQCkWoxt/0vsQvZswzTHUVOImccYmSyhJTp7kGtPed0Qcx8rK9h9YEgx+" crossorigin="anonymous"></script>
  </body>
</html>
